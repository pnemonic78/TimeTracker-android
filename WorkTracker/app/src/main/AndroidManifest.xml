<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tikalk.worktracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkTracker"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".start.SplashActivity"
            android:label="@string/activity_splash"
            android:theme="@style/Theme.WorkTracker.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".auth.LoginActivity"
            android:label="@string/activity_login" />
        <activity
            android:name=".auth.BasicRealmActivity"
            android:label="@string/activity_basic_realm" />
        <activity
            android:name=".time.TimeEditActivity"
            android:label="@string/activity_time" />
    </application>

</manifest>