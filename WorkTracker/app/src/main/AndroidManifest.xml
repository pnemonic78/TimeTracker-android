<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tikalk.worktracker">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-sdk tools:overrideLibrary="androidx.security" />

    <application
        android:name="com.tikalk.worktracker.app.TrackerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkTracker"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".start.SplashActivity"
            android:label="@string/activity_splash"
            android:theme="@style/Theme.WorkTracker.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.tikalk.worktracker.auth.LoginActivity"
            android:label="@string/activity_login" />
        <activity
            android:name="com.tikalk.worktracker.time.TimeListActivity"
            android:label="@string/activity_time" />
        <activity
            android:name="com.tikalk.worktracker.time.TimeEditActivity"
            android:label="@string/activity_time"
            android:parentActivityName=".time.TimeListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".time.TimeListActivity" />
        </activity>

        <receiver android:name=".time.TimeReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="${applicationId}.STOP" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/export_paths" />
        </provider>
    </application>

</manifest>